# Bu dosyayı .env olarak kopyala ve doldur:  cp .env.example .env
# .env .gitignore'da — gerçek şifre repoya girmez.

FTP_HOST=ftp.alanadin.com
FTP_USER=kullanici_adi
FTP_PASS=sifre

# Opsiyonel (varsayılanlar):
# FTP_PORT=21
# REMOTE_DIR=/public_html
